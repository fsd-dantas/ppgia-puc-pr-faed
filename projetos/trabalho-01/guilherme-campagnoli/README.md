# guilherme-campagnoli — Implementação

Contribuição de guilherme-campagnoli para o Trabalho 01.

## Arquivos

| Arquivo | Descrição |
|---|---|
| *(adicionar aqui)* | *(descrição)* |

## Pré-requisitos

- Python 3.13+
- Dependências: *(listar se necessário)*

## Como executar

```bash
# Adicionar instruções de execução aqui
```

## Dados gerados

Os CSVs são salvos em `dados/` (excluído do git — reproduzível via script).
Para usar os mesmos dados dos outros membros, importar `data_generator.py` de `fsd-dantas/`:

```python
import sys
sys.path.insert(0, "../fsd-dantas")
from data_generator import gerar_registros, carregar_csv
```

## Design

*(descrever decisões de implementação aqui)*
